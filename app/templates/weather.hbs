{{typeahead-component name='name' searchMessage="searchHandler"}}

{{weather-current weather=model
                  saved=isSavedWeather
                  locations=locations
                  saveLocationHandler="saveLocation"
                  removeLocationHandler="removeLocation"}}
